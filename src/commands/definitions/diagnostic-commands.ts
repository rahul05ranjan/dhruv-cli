import type { BuiltInCommand } from '../built-in-commands.js';

/** Diagnostics, setup and remaining commands (init … completion). Migrated in #128. */
export const diagnosticCommands: readonly BuiltInCommand[] = [];
