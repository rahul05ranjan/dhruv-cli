import type { BuiltInCommand } from '../built-in-commands.js';

/** Source-driven commands (review, optimize, security-check, generate). Migrated in #127. */
export const sourceCommands: readonly BuiltInCommand[] = [];
